# Lockplane configuration for Lockplane Todos

default_environment = "local"

[environments.local]
  description = "Local development with Turso"
  # Loads DATABASE_URL and SHADOW_DATABASE_URL from .env.local

[environments.prod]
  description = "Production Turso database"
  # Loads DATABASE_URL and SHADOW_DATABASE_URL from .env.prod
